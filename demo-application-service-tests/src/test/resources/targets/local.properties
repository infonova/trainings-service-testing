## generic
hostname = localhost
wiremock.host = ${hostname}
wiremock.port = 8181

rest.wiremock.admin.host = ${wiremock.host}
rest.wiremock.admin.port = ${wiremock.port}
rest.wiremock.url = http://${wiremock.host}:${wiremock.port}/ws

## demo application
rest.demo.url = http://${hostname}:8080/demo